# Python dependencies for the Volterra AI backend service.
# Pinned to the versions actually installed in the dev venv (Python 3.11).
fastapi==0.136.3
uvicorn[standard]==0.48.0
httpx==0.28.1
yfinance==1.4.1
pandas==3.0.3
anthropic==0.105.2
apscheduler==3.11.2
python-dotenv==1.2.2
sqlalchemy==2.0.50
asyncpg==0.31.0
pydantic==2.13.4
PyJWT==2.13.0
cryptography==50.0.0  # required by PyJWT for ES256/RS256 (Supabase JWKS verification)
resend==2.30.1
sentry-sdk==2.62.0
slowapi==0.1.9
pytest==9.0.3
pytest-asyncio==1.4.0
# NOTE: the Upstash cache (cache.py) uses its REST API over httpx, so the
# redis client library is not required. The supabase client SDK is also not
# used server-side — auth is verified locally with PyJWT.
